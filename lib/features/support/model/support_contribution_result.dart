/// Outcome of tapping Support on the existing Support DeenFocus screen.
enum SupportContributionResult {
  purchased,
  cancelled,
  pending,
  failed,
  productUnavailable,
  launchedExternally,
  launchUnavailable,
}

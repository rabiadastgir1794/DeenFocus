/// Interactive App Lock Demo phases inside onboarding.
enum AppLockDemoPhase {
  intro,
  homeScreen,
  openingApp,
  prayerLock,
  streakReward,
  completion,
  /// Settings-only: ask to enable the real Focus Mode after the walkthrough.
  enableOffer,
}
